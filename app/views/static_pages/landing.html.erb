<content class="landing">

  <section class="landing-title">
    <h1>QUENCHD</h1>
    <h2>DRINK UP!!!</h2>
  </section>

  <section class="landing-descriptions">
    <h4>DISCOVER &amp SHARE</h4>
    <h4>YOUR FAVORITE BEERS</h4>
  </section>

  <section class="landing-action">
    <button class="landing-signup"><a href="<%= new_user_url %>">Sign Up</a></button>
    <form for="guest-sign-in" action="<%= session_url %>" method="POST">
      <input type="hidden" name="user[username]" value="guest">
      <input type="hidden" name="user[password]" value="testing">
      <input class="guest-sign-in-button" type="submit" value="Guest Sign In">
    </form>
    <h5>Already a Member? <a href="<%= new_session_url %>">Sign In!</a></h5>
  </section>

</content>
